---
layout: base.njk
---

{{ content | safe }}

{% set groupedTalks = talks | groupTalksByYear %}

<p class="events">I gave {{ groupedTalks.stats.past }} talks ({{ groupedTalks.stats.keynotes }} keynotes) in {{ groupedTalks.stats.cities }} different cities from {{ groupedTalks.stats.countries }} unique countries, did {{ groupedTalks.livestreamStats.past }} livestream{% if groupedTalks.livestreamStats.past != 1 %}s{% endif %}, was on {{ groupedTalks.podcastStats.past }} podcast{% if groupedTalks.podcastStats.past != 1 %}s{% endif %}, and presented {{ groupedTalks.webinarStats.past }} webinar{% if groupedTalks.webinarStats.past != 1 %}s{% endif %}.</p>

<p class="note"><em>Note: This list is not complete yet. I'm still adding past speaking engagements.</em></p>

<div class="tabs-container">
  <div class="tabs">
    <button class="tab-button active" data-tab="talks">Talks</button>
    <button class="tab-button" data-tab="livestreams">Livestreams</button>
    <button class="tab-button" data-tab="podcasts">Podcasts</button>
    <button class="tab-button" data-tab="webinars">Webinars</button>
  </div>

  <!-- Talks Tab Content -->
  <div id="talks-tab" class="tab-content active">

    <h2 id="upcoming">Upcoming</h2>
    {% if groupedTalks.stats.upcoming > 0 %}
      <p>{{ groupedTalks.stats.upcoming }} events</p>
      <div class="talks">
        {% for event in groupedTalks.upcoming %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% else %}
      <span class="count">(no event)</span>
      <p>More speaking gigs coming soon, stay tuned!</p>
    {% endif %}

    {% for year in groupedTalks.pastYears %}
      <h2 id="past-{{ year }}">{{ year }}</h2>
      <p>{{ groupedTalks.past[year].length }} event{% if groupedTalks.past[year].length != 1 %}s{% endif %}</p>
      <div class="talks">
        {% for event in groupedTalks.past[year] %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% endfor %}
  </div>

  <!-- Livestreams Tab Content -->
  <div id="livestreams-tab" class="tab-content">
    <h2 id="upcoming-livestreams">Upcoming</h2>
    {% if groupedTalks.livestreamStats.upcoming > 0 %}
      <p>{{ groupedTalks.livestreamStats.upcoming }} livestream{% if groupedTalks.livestreamStats.upcoming != 1 %}s{% endif %}</p>
      <div class="talks">
        {% for event in groupedTalks.upcomingLivestreams %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% else %}
      <span class="count">(no livestream)</span>
      <p>More livestreams coming soon, stay tuned!</p>
    {% endif %}

    {% for year in groupedTalks.pastLivestreamYears %}
      <h2 id="past-livestream-{{ year }}">{{ year }}</h2>
      <p>{{ groupedTalks.pastLivestreams[year].length }} livestream{% if groupedTalks.pastLivestreams[year].length != 1 %}s{% endif %}</p>
      <div class="talks">
        {% for event in groupedTalks.pastLivestreams[year] %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% endfor %}
  </div>

  <!-- Podcasts Tab Content -->
  <div id="podcasts-tab" class="tab-content">
    <h2 id="upcoming-podcasts">Upcoming</h2>
    {% if groupedTalks.podcastStats.upcoming > 0 %}
      <p>{{ groupedTalks.podcastStats.upcoming }} podcast{% if groupedTalks.podcastStats.upcoming != 1 %}s{% endif %}</p>
      <div class="talks">
        {% for event in groupedTalks.upcomingPodcasts %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% else %}
      <span class="count">(no podcast)</span>
      <p>More podcast appearances coming soon, stay tuned!</p>
    {% endif %}

    {% for year in groupedTalks.pastPodcastYears %}
      <h2 id="past-podcast-{{ year }}">{{ year }}</h2>
      <p>{{ groupedTalks.pastPodcasts[year].length }} podcast{% if groupedTalks.pastPodcasts[year].length != 1 %}s{% endif %}</p>
      <div class="talks">
        {% for event in groupedTalks.pastPodcasts[year] %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% endfor %}
  </div>

  <!-- Webinars Tab Content -->
  <div id="webinars-tab" class="tab-content">
    <h2 id="upcoming-webinars">Upcoming</h2>
    {% if groupedTalks.webinarStats.upcoming > 0 %}
      <p>{{ groupedTalks.webinarStats.upcoming }} webinar{% if groupedTalks.webinarStats.upcoming != 1 %}s{% endif %}</p>
      <div class="talks">
        {% for event in groupedTalks.upcomingWebinars %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% else %}
      <span class="count">(no webinar)</span>
      <p>More webinars coming soon, stay tuned!</p>
    {% endif %}

    {% for year in groupedTalks.pastWebinarYears %}
      <h2 id="past-webinar-{{ year }}">{{ year }}</h2>
      <p>{{ groupedTalks.pastWebinars[year].length }} webinar{% if groupedTalks.pastWebinars[year].length != 1 %}s{% endif %}</p>
      <div class="talks">
        {% for event in groupedTalks.pastWebinars[year] %}
          {% include "talk-item.njk" %}
        {% endfor %}
      </div>
    {% endfor %}
  </div>
</div>
